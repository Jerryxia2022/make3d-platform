# Make3D Changelog

## Unreleased

- Phase05-J0 release candidate preparation: created `codex/phase05-j0-rc` from production baseline `5566384`, narrowed the candidate to Phase05 Worker Slicing/API/schema/Worker/tests/scripts/reports, excluded unrelated local artifacts and secrets/logs/databases, added the release candidate file list, rehearsed candidate schema idempotency on a temporary SQLite database, verified WSL PrusaSlicer/profile/file-sync-worker state, and passed `npm test` 335/335 plus lint/build; Phase05-J1 is allowed only for cloud API/schema staged deployment, not production Slicing Worker execution.
- Phase05-I production pre-deploy audit: inspected local candidate, production git/Docker/Nginx/runtime, production SQLite health and an audit backup, production env presence without secret output, WSL PrusaSlicer/Worker host state, Worker Slicing schema/API/resource/log boundaries, and full local tests; `npm test` 336/336 plus lint/build passed, but direct deployment is blocked until the candidate is rebased/committed on production HEAD, migration is rehearsed against the exact target commit, Worker resource guards are added before service enablement, and `/srv/make3d-worker/results/prusaslicer` is prepared.
- Phase05-H-C final lease process fencing hardening: anchored lock/lease local deadlines to request-start monotonic time with a 2000 ms safety margin, deducted network RTT, added shutdown ownership fencing so SIGTERM/SIGINT cleanup does not post failed state, kept SIGKILL cleanup alive, added same-filesystem atomic publish checks, added local-only sanitized request logging, added 18 lease fencing tests, and passed isolated WSL integration covering lease-loss slow slicer parent/child cleanup, Worker SIGTERM cleanup, long parser lease renewal, and resume parser lease renewal; `npm test` 336/336 plus lint/build passed, existing file sync Worker stayed active on PID 2682, and no production/order/quote/payment/WeChat/upload/slicing-systemd deployment change was made.
- Phase05-H-C attempt isolation and lease ownership fencing: upgraded Worker slicing artifacts to `attempt-<attempt_no>` processing/result paths, made `/sliced` accept only current server-side attempt paths, restricted resume artifact paths to verified same-job historical attempts from the same artifact Worker, added `.part` temporary outputs with atomic publish, added monotonic server-TTL lease ownership tracking, continued heartbeat through result/failed, added best-effort PrusaSlicer process cleanup, added 18 H-C tests plus local isolated H-C integration, verified `npm test` 318/318 plus lint/build, and confirmed existing file sync Worker stayed active on PID 2682; no production deployment, production DB/token, real customer file/task, slicing systemd, order/quote/payment/WeChat/upload change.
- Phase05-H-B local failure/retry/recovery validation: added Worker slicing resume support for `resume_from=sliced` and `resume_from=parsing`, lock response artifact diagnostics, isolated H-B fault injection and integration scripts, and recovery tests for retryable/non-retryable failure, lock/slicing/sliced/parsing expiry, different artifact Worker ownership, lease heartbeat, missing resume artifacts, and G-code reuse; local integration passed with existing file sync Worker still active on PID 2682, `npm test` 300/300 plus lint/build passed, and no production deployment, production DB/token, real customer file/task, slicing systemd, order/quote/payment/WeChat/upload change.
- Phase05-H-A lease contract correction: replaced the Worker Slicing 300000 ms default lock/lease with single `WORKER_SLICING_LEASE_DURATION_MS = 120000`, removed route-level lease hardcoding, exposed lock timing diagnostics, re-ran local closed-loop integration with new synthetic TEST task `M3D20260715171600827`, verified initial lock and lease deltas are both 120000 ms, confirmed existing file sync Worker stayed active, and verified targeted tests plus `npm test` 289/289, lint, and build; no production deployment, production DB/token, real customer file/task, slicing systemd service, order/quote/payment/WeChat/upload change.
- Phase05-H-A local closed-loop Worker slicing integration: added independent one-shot `worker/make3d-slicing-worker.mjs`, local integration/seed/verify scripts, and `tests/workerSlicingClient.test.mjs`; completed local TEST SQLite -> API -> WSL Worker -> PrusaSlicer -> parser -> result flow with final `partial`, G-code SHA verification, token cleanup, and existing file sync Worker still active; verified `npm test` 286/286 plus lint/build; no production deployment, production DB/token, real customer file/task, order/quote/payment/WeChat/upload change, or slicing systemd service.
- Phase05-G API verification hardening: audited the original five slicing API tests, expanded `tests/workerSlicingApi.test.mjs` to 22 route-level tests covering auth, body limits, headers, pending/lock/lease/state/result/failed/reconcile/idempotency/non-interference branches, hardened Worker failure error-code handling and sanitized truncation, and verified `npm test` 273/273 plus lint/build; no WSL Worker, PrusaSlicer execution, real slicing, order/quote/payment/WeChat/upload, production token, production DB, or deployment change.
- Phase05-G Worker Slicing API implementation: added cloud-side slicing Worker authentication, request/response/body/schema helpers, pending/lock/lease/slicing/sliced/parsing/result/failed routes, retry/reconcile/resume_from logic, parser-derived result validation, terminal replay/idempotency handling, and `tests/workerSlicingApi.test.mjs`; tests pass with `npm test` 256/256, lint, and build; no WSL Worker change, PrusaSlicer execution, real slicing task, order/quote/payment/WeChat/upload change, production token change, production DB access, or deployment.
- Phase05-F implementation specification addendum: froze parser-derived `/result` schema, metric/source/validation field limits, NULL-vs-zero semantics, parser consistency validation, Worker/server error code tables, validation-failure state transitions, authoritative `resume_from` flow, final lock response/request rules, job id and lock owner validation, idempotency normalization, and implementation test checklist; design-only, no API/Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F final contract freeze: froze the Worker slicing API result matrix, server-validated parser quote readiness, expired-state reconciliation, retryable failed pending behavior, `resume_from`, restricted resume path, terminal replay through attempt records, strict `/sliced` success criteria, error code policy, content type/encoding rules, and final implementation test checklist; design-only, no API/Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F API design revision: tightened `/result` terminal matrix, server-side parse cache key calculation, job-bound artifact paths, expired lease recovery, heartbeat lease semantics, normalized idempotency comparison, strict JSON schema/body limits, ownership hiding, no-store response policy, Worker error code policy, and constant-time token comparison; design-only, no API implementation, Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F Worker Slicing API design: drafted slicing Worker authentication, route contracts, request/response schemas, state transitions, lock replay, lease, version/SHA validation, path safety, result/error/idempotency/logging rules, and test plan; no API implementation, Worker change, PrusaSlicer execution, quote/payment/upload change, or deployment.
- Phase05-E atomic idempotency hardening: added partial unique active slicing identity index, changed `createSlicingJobForVerifiedFile` to return `{ job, created }`, handled active identity race conflicts idempotently, and updated Phase05-E report totals to `npm test` 251/251; no API, Worker, PrusaSlicer execution, quote, payment, upload, or deployment change.
- Phase05-E constraint correction: removed incorrect unique `file_sync_job_id` slicing index, added ordinary `(file_sync_job_id, created_at)` index, implemented active-task idempotency by file/cache/parser identity, and added same-file multi-parameter/cache-reuse tests; no API, Worker, quote, payment, upload, or deployment change.
- Phase05-E slicing jobs database helpers: added `slicing_jobs` and `slicing_job_attempts`, stable slice/parse cache keys, verified-file task creation, transactional lock/lease/attempt/state/result/failure helpers, metrics-cache reuse, and 43 DB/helper tests after atomic idempotency hardening; no API route, Worker slicing execution, quote/payment/upload change, or deployment.
- Phase05-D schema freeze: finalized Worker input/artifact ownership, required vs actual slicer/parser versions, result origin, Unix-millisecond Worker timing, transactional lock/attempt/lease/result boundaries, stable cache-key algorithms, pending API `slice_params` object shape, and the Phase05-E readiness checklist; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-D design revision: split slice and parse cache keys, added Worker lease renewal, attempt audit table, profile_key whitelist delivery, metrics-only cache reuse, `/lease` and `/result` API design, `parser_quote_ready` naming, and stronger database constraints; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-D slicing jobs database and Worker state design: designed future `slicing_jobs` audit table, independent slicing state machine, atomic lock/retry model, cache lineage, G-code retention boundaries, Worker slicing API shape, and quote/material-weight boundaries; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-C slicing result parser implementation: added pure PrusaSlicer G-code parser, normalized slice parameter fingerprinting, metric source/validation JSON, guarded file access, and 24 parser tests; no database migration, quote integration, payment change, or deployment.
- Phase05-C slicing result parsing design: analyzed the synthetic TEST G-code tail/header fields, designed parser JSON, unit normalization, slicing_jobs schema, idempotency/cache rules, and parser safety boundaries without implementing parser or database migrations.
- Phase05-B PrusaSlicer CLI validation: fixed install script version detection, validated synthetic 20mm cube slicing under `make3d-worker`, generated G-code only in `/srv/make3d-worker/test-slicer`, and confirmed tests/lint/build pass without entering automatic quote.
- Phase05-A VPN-off install retry: pre-install check passed, but network check stopped because `security.ubuntu.com` timed out; apt update and PrusaSlicer install were not run.
- Phase05-B1 temporary WSL proxy validation: confirmed shell-only proxy reaches Windows proxy but Ubuntu official APT TLS still fails; PrusaSlicer was not installed and no persistent network config was changed.
- Phase05-A network diagnostics: confirmed WSL NAT mode with fake-ip DNS and failing Ubuntu official APT connectivity; generated network fix design without changing proxy, DNS, firewall, production, payment, upload, or quote settings.
- Phase05-A install validation: PrusaSlicer official Ubuntu APT install was blocked by WSL network/proxy access to archive.ubuntu.com; local PrusaSlicer working directories were created, no slicing or production changes performed.
- Phase05-A started: WSL environment check and PrusaSlicer install/CLI design reports created; PrusaSlicer not installed.
- Phase04-A implementation: added WSL local file sync Worker, systemd unit, install script, local heartbeat, and Worker sync tests; production was not deployed.
- Phase04-A started: WSL local file sync Worker design report created; implementation pending approval.
- Phase03 implementation: added cloud Worker API support and future-order `local_file_sync_jobs` creation without historical `files` backfill.
- Phase03 started: Worker API design report created for cloud-side local file sync API planning.
- Established project report, deployment report, and changelog directories.
