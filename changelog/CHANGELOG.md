# Make3D Changelog

## Unreleased

- Phase06-A3-C1 parser metrics completeness, validation semantics, and real-order preflight: hardened the PrusaSlicer parser in the Phase06 release worktree so unknown material weight is `null` instead of `0`, direct `filament used [g]` and calculated length/diameter/density weights are supported, `filament_density=0` can use a documented project material default density fallback for PLA with NON_BLOCKING warning, dimensions now record `gcode_bounds` or `cloud_file_geometry`, parser status uses `ok/warning/error`, local Workbench invalid draft submissions return HTTP 422 while preserving previous valid drafts, and Worker Slicing API payloads remain server-field-whitelisted while accepting the new status/source values; added deterministic project-owned `tests/fixtures/prusaslicer/20mm-cube.stl` plus generator, verified real WSL PrusaSlicer fixture slicing under `make3d-worker` with G-code `284994` bytes, SHA `11760c078333153a77d6b90d2fcd1e9a8c806983691890489dee2751e0d5d1ac`, `print_time_seconds=1496`, `material_weight_grams=6.313`, `metrics_status=ok`, and no process/flock/.part residue; production read-only counts before/after stayed unchanged with `integrity_check=ok` and `foreign_key_check_count=0`; real-order read-only preflight found no eligible non-TEST verified/local-synced STL/3MF candidate, so Phase06-A3-C2 is not yet recommended; verified focused regression 137/137, recovery 8/8, full `npm test` 393/393, lint, and build; generated `reports/phase06-a3-c1-parser-metrics-and-real-order-preflight-final.md`; no production deployment, production env/DB write, online order/quote/lead-time/message/payment/refund/WeChat/upload change, real customer slicing, online `slicing_job`, resident Slicing Worker, or slicing systemd service was performed.
- Phase06-A2-A Local Order Workbench read-only MVP implementation: added dedicated `MAKE3D_LOCAL_WORKBENCH_TOKEN` authenticated read-only operator workbench APIs (`GET /api/operator/workbench/orders` and `GET /api/operator/workbench/orders/:id`), a localhost-only `worker/order-workbench` server bound to `127.0.0.1` by default, safe order list/detail rendering, local file existence/size/SHA verification under `/srv/make3d-worker/files`, guarded POST-only open-directory action with Host/Origin/CSRF checks, security headers, field allowlists, and redaction tests; verified new tests 11/11, focused Operator Console/file-sync Worker/Worker API/Worker Slicing regression 55/55, full `npm test` 398/398, lint, and build; generated `reports/phase06-a2-a-local-order-workbench-readonly-mvp-implementation-final.md`, with no production deployment, production env change, schema migration, production DB write, PrusaSlicer run, `slicing_job`, resident Worker, order/quote/lead-time/payment/refund/WeChat/upload change, or secret output.
- Phase06-A1 Local Order Workbench audit and MVP plan: paused Phase05-L4-B Approval / Production Candidate schema migration work, audited existing order/detail/customer page/quote/lead-time/message/notification/file-sync/Worker Slicing/Operator Console/admin-auth surfaces, designed a localhost-only `127.0.0.1` local order workbench MVP with online order pull, local file sync verification, safe local file opening under `/srv/make3d-worker/files`, manual one-shot slicing reuse, guarded price/lead-time/reply sync, conflict/idempotency controls, customer-visible order replies, station/email notification plan, and staged A2-A5 rollout; generated `reports/phase06-a1-local-order-workbench-audit-and-mvp-plan-final.md` only, with no production deployment, schema migration, Worker start, PrusaSlicer run, order/quote/payment/refund/WeChat/upload/customer data change, or secret output.

- Phase05-L3 production read-only migration rehearsal: created production source backup `/opt/make3d-platform/backups/make3d.db.phase05-l3-rehearsal-source.20260717-121602.bak` and working copy `/opt/make3d-platform/backups/make3d.db.phase05-l3-rehearsal-working.20260717-121602.db`, verified live DB read-only baseline and post-check stayed unchanged (`integrity_check=ok`, `foreign_key_check_count=0`, business counts `orders=22`, `files=24`, `local_file_sync_jobs=13`, `slicing_jobs=13`, `slicing_job_attempts=21`, `order_payments=8`, `wechat_refunds=3`, `payment_settings=1`, live SHA unchanged `a890870d23c4dc369dd683d92d110cf7e74e38b8f590a3eb6c8c7af6a40ee749`, and no L1/L2 tables present), rehearsed `applyApprovalCandidateSchema` twice only on the working copy, verified all three new tables and ten indexes including partial active identity unique index, verified new table rows remain 0 and business counts unchanged, confirmed no Worker/PrusaSlicer/Slicing Worker service, and passed L2 tests 24/24, focused regression 116/116, `npm test` 382/382, lint, and build; generated `reports/phase05-l3-production-readonly-migration-rehearsal-final.md`, with no production deployment, live DB migration/write, real approval, real Candidate, real `slicing_job`, order/quote/price/payment/WeChat/upload/customer change, Docker/Nginx restart, Worker start, or PrusaSlicer run.
- Phase05-L2 local-only schema/helper implementation: added local-only Approval + Production Candidate helper modules for additive schema creation, canonical JSON/SHA-256 hashing, sensitive snapshot guards, approval audit creation, production candidate creation, candidate state transitions, and candidate audit events; added temporary-SQLite tests and a local integration script proving idempotent migration, active candidate duplicate handling, approved/rejected/request-change boundaries, state machine enforcement, audit event insertion, snapshot immutability, and no `slicing_jobs` writes; verified new L2 tests 24/24, focused Operator/Worker regression 116/116, full `npm test` 382/382, lint, and build; generated `reports/phase05-l2-local-schema-helper-implementation-final.md`, with no production deployment, production database migration/write, real approval, real Candidate, real `slicing_job`, Worker start, PrusaSlicer run, systemd service, order/quote/price/payment/WeChat/upload/customer change, or schema auto-attachment to app startup.
- Phase05-L1 Approval + Production Candidate schema freeze: froze the future additive `approval_audit_records`, `production_candidates`, and `production_candidate_audit_events` contracts, including append-only approval/candidate audit rules, active candidate uniqueness by `order_id + file_snapshot_sha256 + quote_snapshot_sha256`, canonical JSON/SHA-256 hash rules, candidate transition matrix, role boundaries for `READONLY_OPERATOR`, `APPROVAL_OPERATOR`, `PRODUCTION_OPERATOR`, and `ADMIN`, helper/API contract design, migration design, and future tests; generated `reports/phase05-l1-approval-candidate-schema-freeze-final.md` only, with no production deployment, production database migration/write, real approval, real Candidate, `slicing_job`, Worker start, PrusaSlicer run, systemd service, order/quote/price/payment/WeChat/upload/customer change, or secret output.
- Phase05-L Production Candidate workflow design: designed the isolation layer between Approval and Slicing Execution with a future `production_candidates` model, candidate snapshot fields for files, quote, risk, material/color/quantity, and profile, `CREATED -> READY_FOR_PRODUCTION -> MANUAL_EXECUTION_STARTED -> COMPLETED / CANCELLED` lifecycle, active uniqueness by `order_id + file_snapshot_sha256 + quote_snapshot_sha256`, append-only candidate audit events (`create`, `mark_ready`, `cancel`, `manual_start`, `complete`), future `PRODUCTION_OPERATOR` boundaries, and the rule that a Production Candidate is not an order, payment, shipment, slicing job, Worker run, PrusaSlicer run, or quote recalculation; generated `reports/phase05-l-production-candidate-workflow-design-final.md` only, with no production code, deployment, database migration/write, real Candidate, `slicing_job`, Worker start, PrusaSlicer run, systemd service, order/quote/price/payment/WeChat/upload/customer change, or real customer slicing.
- Phase05-K-F Manual real-customer approval design: designed the future real-customer approval workflow with `READONLY_OPERATOR`, future `APPROVAL_OPERATOR`, and `ADMIN` role boundaries, a `PENDING_REVIEW -> APPROVED / REJECTED / NEED_CUSTOMER_CONFIRM` approval state machine, append-only approval audit record requirements, pre-production risk checks for files, quotes, orders, and production limits, disabled Local Operator Approval View actions, and the rule that approval does not create a slicing job, production candidate, Worker run, PrusaSlicer run, quote/order/payment update, or automatic production action; generated `reports/phase05-k-f-manual-real-customer-approval-design-final.md` only, with no production deployment, production database write, Worker start, PrusaSlicer run, systemd service, real customer slicing, order/price/payment/WeChat/upload change, or test execution required for this design-only phase.
- Phase05-K-E Manual real-customer read-only preview: added a local Operator Console `READONLY_OPERATOR` preview path using read-only SQLite access and `GET`-only semantics, with masked filenames, SHA prefixes, existing quote/slice/slicing metadata display, readiness/risk indicators, and disabled action markers for create slicing job, run Worker, run PrusaSlicer, update order, update quote, and update payment; added `worker/operator-console/lib/readonlyPreview.mjs`, `--readonly-preview --db <sqlite-path>`, and tests proving real orders can be read, TEST orders are not executable work, write attempts fail on the read-only handle, table counts remain unchanged, and sensitive fields are not emitted; production was observed read-only at HEAD `052ab1aa28676087f246b2d4659048868cdb5147` with no real non-TEST orders returned, counts unchanged (`orders=22`, `files=24`, `local_file_sync_jobs=13`, `slicing_jobs=13`, `slicing_job_attempts=21`, `order_payments=8`, `wechat_refunds=3`, `payment_settings=1`), `integrity_check=ok`, `foreign_key_check=0`, file sync Worker still active on PID `203`, no Slicing Worker/PrusaSlicer process or service, and `npm test` 358/358, lint, and build passed.
- Phase05-K-D TEST-only production one-shot through Console: backed up production SQLite to `/opt/make3d-platform/backups/make3d.db.phase05-k-d-before-console-oneshot.20260717-094824.bak`, confirmed production HEAD `052ab1aa28676087f246b2d4659048868cdb5147`, Docker/Nginx healthy, file sync Worker active on PID `203`, no pending/active slicing jobs, no PrusaSlicer/Slicing Worker residual process, then added one marked `PHASE05_K_D_TEST` synthetic order/file/local sync/slicing job (`order 41`, `file 45`, sync job `13`, slicing job `13`) and triggered exactly one guarded real one-shot through Operator Console using `MAKE3D_OPERATOR_CONSOLE_ALLOW_REAL_RUN=1` plus confirmation `PHASE05_K_D_TEST`; Console classified the job as `TEST_SAFE`, Worker completed `pending -> lock -> slicing -> sliced -> parsing -> result` with final `partial`, attempt `1`, G-code SHA `e54601f39cb23a884d3092c33239d0cad1586024e5f5b820d1439619cb85d08d`, size `284994`, then Console empty queue returned `no-task` with `prusaSlicerRan=false`; DB `integrity_check=ok`, `foreign_key_check=0`, payment/refund/settings counts unchanged, no Slicing Worker service/persistent Worker/real customer task/auto quote/order/payment/WeChat/upload/pricing change, and local `npm test` 354/354, lint, and build passed.
- Phase05-K-C TEST-only Console dry run: ran the WSL/local Operator Console dry-run validation as `make3d-worker` without exposing env contents or Worker Token, verified CLI menu output, System Health `OK`, API config present, Worker Token present, PrusaSlicer available, profile SHA `4437bf3e44534004aa51db7c6de16c13c130f62de3cd3b14d52194a7eb4f6e0f`, file sync Worker active on PID `203`, no active Slicing Worker service, no PrusaSlicer or Slicing Worker residual process, read-only pending API returned `jobs=[]`, classifier fixtures covered `TEST_SAFE`, `BLOCKED_REAL_CUSTOMER`, `MANUAL_APPROVAL_REQUIRED`, and `UNKNOWN_BLOCKED`, one-shot remained dry-run/blocked with command shape only, empty queue check returned simulated `no-task`, generated sanitized JSON/Markdown dry-run reports under `/tmp`, sensitive scan counts were 0, and verified Operator Console tests 16/16, Worker regression tests 97/97, full `npm test` 353/353, lint, and build; no production deployment, push, production DB/env/token change, production slicing job, real customer file/task, real one-shot Worker, persistent Slicing Worker, systemd service, public API, web console, order/quote/payment/WeChat/upload/customer-status change, or secret output.
- Phase05-K-B local Operator CLI prototype: added a WSL/local-only Operator Console CLI skeleton with system health, pending queue viewing, job classification, one-shot dry-run planning, empty queue simulation, report generation, shared redaction, safe relative path validation, and TEST_SAFE-only runner preflight; added five mock-based test files covering token/auth/lock_owner/OpenID/phone/email redaction, pending field allowlisting, report sanitization, path rejection, TEST_SAFE vs blocked classification, dry-run no child process behavior, and CLI menu startup; verified targeted Operator Console tests 16/16, Worker regression tests 97/97, full `npm test` 353/353, lint, and build; no production deployment, production database/env/token change, production one-shot Worker run, slicing job creation, public API, web console, systemd service, order/quote/payment/WeChat/upload/customer-status change, or secret output.
- Phase05-K local operator console design: designed a WSL/local-only operator console for Worker Slicing health checks, pending queue inspection, TEST_SAFE classification, one-shot execution, empty-queue checks, result inspection, and sanitized local audit reports; recommended a CLI prototype first, deferred any localhost-only web console until after CLI validation, rejected public/admin Next.js controls for now, kept real-customer slicing blocked, and made no code, deployment, production database, environment, token, Worker service, order, quote, payment, WeChat Pay, upload, or customer-status changes.
- Phase05-J Stage 2-F TEST-only safe input path slicing revalidation: created a new TEST synthetic 20 mm cube input at `files/phase05-j-stage2f-safe-input-cube-20260716-174927.stl` with non-fallback naming, created only marked TEST synthetic records (`order 31`, `file 35`, sync job `3`, slicing job `3`) through project helpers including `createSlicingJobForVerifiedFile`, verified pending API returned exactly job `3` with `input_relative_path`, `input_size_bytes`, and `input_sha256` and no absolute path/token/OpenID/payment fields, ran one-shot formal Slicing Worker against production API with existing token not printed, completed pending -> lock -> slicing -> PrusaSlicer -> sliced -> parsing -> result with final `partial`, confirmed input SHA/size and G-code SHA `2e15d05616321b8df48c96cf01874fc2e8281aa5244049c8de3508641beb710d`, kept pending empty afterward, preserved Stage 2 historical jobs, DB `integrity_check=ok` and `foreign_key_check=0`, only expected TEST synthetic counts changed, no payment/refund/public WeChat setting changed, no PrusaSlicer or Slicing Worker residual process remained, and no real customer/order/quote/payment/WeChat/upload change was made.
- Phase05-J Stage 2-E safe input path patch production deploy: pushed release commit `052ab1aa28676087f246b2d4659048868cdb5147` to `origin/phase05-worker-slicing-candidate`, fast-forwarded production from `abca39e83bb959b7ba99ffde50d1c8532f9188d0` to `052ab1aa28676087f246b2d4659048868cdb5147`, preserved backup `/opt/make3d-platform/backups/make3d.db.phase05-j-stage2e-before-safe-input-path-patch.20260716-172646.bak` with `integrity_check=ok` and `foreign_key_check=0`, rebuilt and restarted only the actual compose service `make3d` after confirming `make3d-platform` is the container name, verified public pages returned 200, Worker Slicing pending API returned 401/401/200 with `jobs=[]` and no forbidden payload fields, database counts and Stage 2 historical records stayed unchanged, logs showed no 500/database/migration/token/payment/slicer anomalies, no PrusaSlicer or Slicing Worker process/unit was started, and no order/quote/payment/WeChat/upload/env/token/schema change was made.
- Phase05-J Stage 4 TEST-only manual one-shot operational acceptance: backed up production SQLite, confirmed pending was empty, created one marked Stage4 TEST synthetic 20 mm cube task through `createOrderWithFile` and `createSlicingJobForVerifiedFile`, verified pending returned only job `12` with safe `input_relative_path`, manually ran formal `node worker/make3d-slicing-worker.mjs --once` as `make3d-worker`, completed `pending -> lock -> slicing -> PrusaSlicer -> sliced -> parsing -> result` with final `partial`, G-code `284994` bytes and SHA `87d6dfe06b665d75b4be36607e81b77f23d9520c35cc1051c8a6b94984cfbc87`, verified fallback input file absence, reran empty-queue one-shot with `status=no-task` and no new attempt, kept payments/refunds and `payment_settings.wechat_enabled=0` unchanged, DB health `integrity_check=ok` and `foreign_key_check=0`, no Slicing Worker service or residual PrusaSlicer process, and file sync Worker stayed active on PID 203.
- Phase05-J Stage 3-R residual TEST pending job retirement: backed up production SQLite, confirmed pending contained only Stage3 TEST synthetic job `11`, verified job `11` ownership, marker, input path, and attempt history, then used one guarded SQL update to move only job `11` from `failed / WORKER_IO_ERROR` to terminal `cancelled` with `affected_rows=1`; attempts, orders, files, local sync jobs, Stage 2/Stage 2-F/other Stage 3 records, payments/refunds, and `payment_settings.wechat_enabled=0` remained unchanged, pending API now returns `jobs=[]`, DB health is `integrity_check=ok` and `foreign_key_check=0`, no Slicing Worker service or PrusaSlicer process exists, and file sync Worker stayed active on PID 203.
- Phase05-J Stage 3 TEST-only failure/retry/recovery validation: backed up production SQLite, used only `PHASE05_J_STAGE3_TEST` synthetic TEST customer records, validated retryable failure, non-retryable failure, locked/slicing/sliced/parsing lease expiry, `resume_from=sliced`, `resume_from=parsing`, old `lock_owner` fencing across all state endpoints, G-code reuse without PrusaSlicer rerun, and missing resume artifact fail-closed behavior; final DB health is `integrity_check=ok`, `foreign_key_check=0`, payments/refunds and `payment_settings.wechat_enabled=0` unchanged, file sync Worker stayed active on PID 203, no Slicing Worker service or residual PrusaSlicer process exists, and only TEST job 11 remains pending due the expected retryable `WORKER_IO_ERROR` policy.
- Phase05-J Stage 2-D safe input path patch predeploy audit: audited the Stage 2-C local candidate and production read-only state without deploying, restarting, pulling, writing production DB, changing tokens/env, or touching real customer records; production stayed on `abca39e83bb959b7ba99ffde50d1c8532f9188d0`, DB `integrity_check=ok`, `foreign_key_check_count=0`, `local_file_sync_jobs.relative_path` exists as `TEXT NOT NULL` with 2/2 non-empty records, Stage 2 synthetic records remain preserved (`order 30`, `file 34`, sync job `2`, slicing job `1 failed`, slicing job `2 partial`), local WSL file sync Worker stayed active on PID 203, targeted tests plus `npm test` 337/337, lint, and build passed; Stage 2-E is blocked until the Stage 2-C patch is committed as a clear deployable release commit.
- Phase05-J Stage 2-C safe Worker input path contract correction: tightened Worker Slicing pending payloads to include safe `input_relative_path` and `input_size_bytes`, derived the path from verified local sync state without exposing absolute Worker paths, removed formal Worker fallback to `files/<file_id>-synthetic-cube.stl`, limited that fallback to localhost integration test mode only, made input missing/size/SHA/path failures report `/failed`, updated synthetic TEST seed scripts to persist explicit local relative paths, added API/client path contract tests, reran isolated WSL local closed-loop validation with synthetic cube `M3D20260716152520753`, and passed targeted tests plus `npm test` 337/337, lint, and build; no production deploy, production DB/token change, slicing systemd service, real customer file/task, order/quote/payment/WeChat/upload change.
- Phase05-J Stage 2 TEST-only production slicing validation: created a production DB backup, confirmed TEST customer ID 5, used only synthetic 20 mm cube records marked `PHASE05_J_STAGE2_TEST`, created synthetic order `M3D20260716144634592`, file 34, sync job 2, and slicing jobs 1/2; first job failed safely because the synthetic filename did not match the Worker fallback input convention, second one-shot Worker run completed the production API -> WSL Worker -> PrusaSlicer -> parser -> result chain with final `partial`, G-code `284994` bytes and SHA `b1e21073d7f641e08c2c2146b0564dd632782544ba6f0bdaeafc9cfc9ef404f2`, `integrity_check=ok`, `foreign_key_check=0`, no active slicing jobs, no payment/refund change, real non-synthetic order/file/payment/refund counts unchanged, file sync Worker stayed active on PID 203, no resident Slicing Worker or PrusaSlicer process remained, and no order/quote/payment/WeChat/upload logic was changed.
- Phase05-J Stage 1-B read-only validation: observed the Stage 1-A production deployment for about 31 minutes without restarting services or changing production state, verified `make3d-platform` stayed up, Nginx stayed active, file sync Worker stayed active on PID 203, no slicing systemd unit appeared, no PrusaSlicer process appeared, `slicing_jobs=0`, `slicing_job_attempts=0`, `integrity_check=ok`, `foreign_key_check=0`, public pages returned 200, Worker Slicing API returned 401/401/200 with `jobs=[]`, and no 500/database/migration/token/payment anomaly was found.
- Phase05-J Stage 1-A production deployment: deployed exact candidate `abca39e83bb959b7ba99ffde50d1c8532f9188d0` for Worker Slicing API plus additive `slicing_jobs`/`slicing_job_attempts` schema only, preserved backup `/opt/make3d-platform/backups/make3d.db.phase05-j-d-predeploy.20260716-122838.bak`, verified `integrity_check=ok`, `foreign_key_check=0`, Worker Slicing pending API 401/401/200 with empty jobs, `payment_settings.wechat_enabled=0`, `WECHAT_PAY_ENABLED=true`, `WECHAT_PAY_TEST_ONLY=true`, unchanged legacy table counts, existing file sync Worker still active on PID 203, no PrusaSlicer process, no slicing jobs, no Slicing Worker service, and no order/quote/payment/WeChat/upload change.
- Phase05-J-C production cloud API deploy dry plan: documented the Stage 1 cloud API/additive schema-only deployment plan for exact candidate `abca39e83bb959b7ba99ffde50d1c8532f9188d0`, including fresh predeploy backup requirements, live DB initialization plan, exact-commit code deployment approach, Worker Slicing API validation, order/quote/payment/WeChat/file-sync regression checks, log observation, rollback plan, stop conditions, and Worker enablement blockers; no production deploy, git pull, Docker command, Nginx restart, live DB migration, env/token change, slicing job, Slicing Worker start, order/quote/payment/WeChat/upload change, or real customer slicing was performed.
- Phase05-J-B production copy migration rehearsal: used exact candidate `abca39e83bb959b7ba99ffde50d1c8532f9188d0`, created and preserved production audit backup `/opt/make3d-platform/backups/make3d.db.phase05-j-b-copy-rehearsal.20260716-103926.bak`, rehearsed candidate `initDatabase` twice against an isolated copied DB in the running container, verified `integrity_check=ok`, `foreign_key_check=0`, additive creation of `slicing_jobs` and `slicing_job_attempts`, correct partial unique/ordinary indexes, absence of old `idx_slicing_jobs_file_sync_unique`, unchanged old table counts, no public/customer/order/quote/payment slicing-job creation path, production env presence without secret output, existing file sync Worker still active, and cleanup of working copies; no production deploy, push, Docker restart/build, Nginx restart, live DB migration, env/token change, slicing task, or Slicing Worker start was performed.
- Phase05-J-A clean deploy candidate preparation: created `phase05-worker-slicing-candidate` from the Phase05 candidate commit whose merge-base is production `5566384`, backed up local Git state under `C:\Users\21899\Documents\make3d-phase05-candidate-backup\phase05-j-a-20260716-102149`, updated the candidate file list and final report, verified no unrelated quote/order/payment/upload/WeChat changes were added, rehearsed the slicing schema twice on a temporary SQLite database with `integrity_check=ok`, and passed targeted Phase05 tests 163/163 plus `npm test` 335/335, lint, and build; no production deploy, push, Docker command, production DB/env/token change, or Slicing Worker start was performed.
- Phase05-J0 release candidate preparation: created `codex/phase05-j0-rc` from production baseline `5566384`, narrowed the candidate to Phase05 Worker Slicing/API/schema/Worker/tests/scripts/reports, excluded unrelated local artifacts and secrets/logs/databases, added the release candidate file list, rehearsed candidate schema idempotency on a temporary SQLite database, verified WSL PrusaSlicer/profile/file-sync-worker state, and passed `npm test` 335/335 plus lint/build; Phase05-J1 is allowed only for cloud API/schema staged deployment, not production Slicing Worker execution.
- Phase05-I production pre-deploy audit: inspected local candidate, production git/Docker/Nginx/runtime, production SQLite health and an audit backup, production env presence without secret output, WSL PrusaSlicer/Worker host state, Worker Slicing schema/API/resource/log boundaries, and full local tests; `npm test` 336/336 plus lint/build passed, but direct deployment is blocked until the candidate is rebased/committed on production HEAD, migration is rehearsed against the exact target commit, Worker resource guards are added before service enablement, and `/srv/make3d-worker/results/prusaslicer` is prepared.
- Phase05-H-C final lease process fencing hardening: anchored lock/lease local deadlines to request-start monotonic time with a 2000 ms safety margin, deducted network RTT, added shutdown ownership fencing so SIGTERM/SIGINT cleanup does not post failed state, kept SIGKILL cleanup alive, added same-filesystem atomic publish checks, added local-only sanitized request logging, added 18 lease fencing tests, and passed isolated WSL integration covering lease-loss slow slicer parent/child cleanup, Worker SIGTERM cleanup, long parser lease renewal, and resume parser lease renewal; `npm test` 336/336 plus lint/build passed, existing file sync Worker stayed active on PID 2682, and no production/order/quote/payment/WeChat/upload/slicing-systemd deployment change was made.
- Phase05-H-C attempt isolation and lease ownership fencing: upgraded Worker slicing artifacts to `attempt-<attempt_no>` processing/result paths, made `/sliced` accept only current server-side attempt paths, restricted resume artifact paths to verified same-job historical attempts from the same artifact Worker, added `.part` temporary outputs with atomic publish, added monotonic server-TTL lease ownership tracking, continued heartbeat through result/failed, added best-effort PrusaSlicer process cleanup, added 18 H-C tests plus local isolated H-C integration, verified `npm test` 318/318 plus lint/build, and confirmed existing file sync Worker stayed active on PID 2682; no production deployment, production DB/token, real customer file/task, slicing systemd, order/quote/payment/WeChat/upload change.
- Phase05-H-B local failure/retry/recovery validation: added Worker slicing resume support for `resume_from=sliced` and `resume_from=parsing`, lock response artifact diagnostics, isolated H-B fault injection and integration scripts, and recovery tests for retryable/non-retryable failure, lock/slicing/sliced/parsing expiry, different artifact Worker ownership, lease heartbeat, missing resume artifacts, and G-code reuse; local integration passed with existing file sync Worker still active on PID 2682, `npm test` 300/300 plus lint/build passed, and no production deployment, production DB/token, real customer file/task, slicing systemd, order/quote/payment/WeChat/upload change.
- Phase05-H-A lease contract correction: replaced the Worker Slicing 300000 ms default lock/lease with single `WORKER_SLICING_LEASE_DURATION_MS = 120000`, removed route-level lease hardcoding, exposed lock timing diagnostics, re-ran local closed-loop integration with new synthetic TEST task `M3D20260715171600827`, verified initial lock and lease deltas are both 120000 ms, confirmed existing file sync Worker stayed active, and verified targeted tests plus `npm test` 289/289, lint, and build; no production deployment, production DB/token, real customer file/task, slicing systemd service, order/quote/payment/WeChat/upload change.
- Phase05-H-A local closed-loop Worker slicing integration: added independent one-shot `worker/make3d-slicing-worker.mjs`, local integration/seed/verify scripts, and `tests/workerSlicingClient.test.mjs`; completed local TEST SQLite -> API -> WSL Worker -> PrusaSlicer -> parser -> result flow with final `partial`, G-code SHA verification, token cleanup, and existing file sync Worker still active; verified `npm test` 286/286 plus lint/build; no production deployment, production DB/token, real customer file/task, order/quote/payment/WeChat/upload change, or slicing systemd service.
- Phase05-G API verification hardening: audited the original five slicing API tests, expanded `tests/workerSlicingApi.test.mjs` to 22 route-level tests covering auth, body limits, headers, pending/lock/lease/state/result/failed/reconcile/idempotency/non-interference branches, hardened Worker failure error-code handling and sanitized truncation, and verified `npm test` 273/273 plus lint/build; no WSL Worker, PrusaSlicer execution, real slicing, order/quote/payment/WeChat/upload, production token, production DB, or deployment change.
- Phase05-G Worker Slicing API implementation: added cloud-side slicing Worker authentication, request/response/body/schema helpers, pending/lock/lease/slicing/sliced/parsing/result/failed routes, retry/reconcile/resume_from logic, parser-derived result validation, terminal replay/idempotency handling, and `tests/workerSlicingApi.test.mjs`; tests pass with `npm test` 256/256, lint, and build; no WSL Worker change, PrusaSlicer execution, real slicing task, order/quote/payment/WeChat/upload change, production token change, production DB access, or deployment.
- Phase05-F implementation specification addendum: froze parser-derived `/result` schema, metric/source/validation field limits, NULL-vs-zero semantics, parser consistency validation, Worker/server error code tables, validation-failure state transitions, authoritative `resume_from` flow, final lock response/request rules, job id and lock owner validation, idempotency normalization, and implementation test checklist; design-only, no API/Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F final contract freeze: froze the Worker slicing API result matrix, server-validated parser quote readiness, expired-state reconciliation, retryable failed pending behavior, `resume_from`, restricted resume path, terminal replay through attempt records, strict `/sliced` success criteria, error code policy, content type/encoding rules, and final implementation test checklist; design-only, no API/Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F API design revision: tightened `/result` terminal matrix, server-side parse cache key calculation, job-bound artifact paths, expired lease recovery, heartbeat lease semantics, normalized idempotency comparison, strict JSON schema/body limits, ownership hiding, no-store response policy, Worker error code policy, and constant-time token comparison; design-only, no API implementation, Worker/database/PrusaSlicer/order/quote/payment/upload/deployment change.
- Phase05-F Worker Slicing API design: drafted slicing Worker authentication, route contracts, request/response schemas, state transitions, lock replay, lease, version/SHA validation, path safety, result/error/idempotency/logging rules, and test plan; no API implementation, Worker change, PrusaSlicer execution, quote/payment/upload change, or deployment.
- Phase05-E atomic idempotency hardening: added partial unique active slicing identity index, changed `createSlicingJobForVerifiedFile` to return `{ job, created }`, handled active identity race conflicts idempotently, and updated Phase05-E report totals to `npm test` 251/251; no API, Worker, PrusaSlicer execution, quote, payment, upload, or deployment change.
- Phase05-E constraint correction: removed incorrect unique `file_sync_job_id` slicing index, added ordinary `(file_sync_job_id, created_at)` index, implemented active-task idempotency by file/cache/parser identity, and added same-file multi-parameter/cache-reuse tests; no API, Worker, quote, payment, upload, or deployment change.
- Phase05-E slicing jobs database helpers: added `slicing_jobs` and `slicing_job_attempts`, stable slice/parse cache keys, verified-file task creation, transactional lock/lease/attempt/state/result/failure helpers, metrics-cache reuse, and 43 DB/helper tests after atomic idempotency hardening; no API route, Worker slicing execution, quote/payment/upload change, or deployment.
- Phase05-D schema freeze: finalized Worker input/artifact ownership, required vs actual slicer/parser versions, result origin, Unix-millisecond Worker timing, transactional lock/attempt/lease/result boundaries, stable cache-key algorithms, pending API `slice_params` object shape, and the Phase05-E readiness checklist; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-D design revision: split slice and parse cache keys, added Worker lease renewal, attempt audit table, profile_key whitelist delivery, metrics-only cache reuse, `/lease` and `/result` API design, `parser_quote_ready` naming, and stronger database constraints; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-D slicing jobs database and Worker state design: designed future `slicing_jobs` audit table, independent slicing state machine, atomic lock/retry model, cache lineage, G-code retention boundaries, Worker slicing API shape, and quote/material-weight boundaries; no migration, API implementation, Worker change, payment change, or deployment.
- Phase05-C slicing result parser implementation: added pure PrusaSlicer G-code parser, normalized slice parameter fingerprinting, metric source/validation JSON, guarded file access, and 24 parser tests; no database migration, quote integration, payment change, or deployment.
- Phase05-C slicing result parsing design: analyzed the synthetic TEST G-code tail/header fields, designed parser JSON, unit normalization, slicing_jobs schema, idempotency/cache rules, and parser safety boundaries without implementing parser or database migrations.
- Phase05-B PrusaSlicer CLI validation: fixed install script version detection, validated synthetic 20mm cube slicing under `make3d-worker`, generated G-code only in `/srv/make3d-worker/test-slicer`, and confirmed tests/lint/build pass without entering automatic quote.
- Phase05-A VPN-off install retry: pre-install check passed, but network check stopped because `security.ubuntu.com` timed out; apt update and PrusaSlicer install were not run.
- Phase05-B1 temporary WSL proxy validation: confirmed shell-only proxy reaches Windows proxy but Ubuntu official APT TLS still fails; PrusaSlicer was not installed and no persistent network config was changed.
- Phase05-A network diagnostics: confirmed WSL NAT mode with fake-ip DNS and failing Ubuntu official APT connectivity; generated network fix design without changing proxy, DNS, firewall, production, payment, upload, or quote settings.
- Phase05-A install validation: PrusaSlicer official Ubuntu APT install was blocked by WSL network/proxy access to archive.ubuntu.com; local PrusaSlicer working directories were created, no slicing or production changes performed.
- Phase05-A started: WSL environment check and PrusaSlicer install/CLI design reports created; PrusaSlicer not installed.
- Phase04-A implementation: added WSL local file sync Worker, systemd unit, install script, local heartbeat, and Worker sync tests; production was not deployed.
- Phase04-A started: WSL local file sync Worker design report created; implementation pending approval.
- Phase03 implementation: added cloud Worker API support and future-order `local_file_sync_jobs` creation without historical `files` backfill.
- Phase03 started: Worker API design report created for cloud-side local file sync API planning.
- Established project report, deployment report, and changelog directories.
